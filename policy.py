# Các phần quan trọng trong policy.py:

class ACTPolicy:
    def __init__(self):
        # Image encoder (ResNet)
        # Transformer encoder
        # Transformer decoder
        # CVAE components
    
    def forward(self, qpos, image, actions):
        # Training forward pass
        # Tính loss
    
    def __call__(self, qpos, image):
        # Inference - predict actions
